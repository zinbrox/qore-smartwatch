# Copy to .env and fill in. Both are optional: without QORE_ADDR the band is found by scanning.
# macOS gives each BLE device its own UUID; find yours with `python watch.py scan`.
QORE_ADDR=
# Advertised name prefix to scan for (default "PBL Qore")
QORE_NAME=
